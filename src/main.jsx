import React from 'react'
import ReactDOM from 'react-dom/client'
import TotalTonnage from './TotalTonnage.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TotalTonnage />
  </React.StrictMode>,
)
